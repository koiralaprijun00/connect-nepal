import React from 'react';
import { NepalTraversalClient } from '@/components/NepalTraversalClient';

// Server component - no client-side hooks or state
export default function NepalTraversalPage() {
  return <NepalTraversalClient />;
}